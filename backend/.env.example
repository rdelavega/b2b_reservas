DATABASE_URL="postgresql://usuario:password@localhost:5432/reservas_b2b?schema=public"
JWT_SECRET="cambia-esta-clave-en-produccion"
JWT_EXPIRES_IN="15m"
PORT=4000
# Origen(es) del frontend permitido(s) por CORS en producción, separados por coma.
# Vacío/ausente = cualquier origen (uso local).
FRONTEND_URL=
